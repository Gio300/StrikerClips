node_modules
dist
dist-site
.env*
!.env.example
*.log
*.tsbuildinfo
# Amplify manual-deploy bundles (scripts/league_pwa.py --zip output)
/*-deploy*.zip
.codex-*
/artifacts/
/coverage/
/playwright-report/
/test-results/
.DS_Store
.cursor/settings.json

# Tauri / Rust
src-tauri/target
src-tauri/gen/schemas
src-tauri/WixTools

# Local-only download artifacts (e.g. promo videos pulled with yt-dlp)
/local-downloads/
*.mp4.tmp

# Local dev + on-device verification helpers (this session)
_*
/android
/appicons

# Android release signing — NEVER commit a keystore or its passwords.
android/keystore.properties
*.jks
*.keystore
*.p8
*.p12
*.mobileprovision
*.ipa
*.xcarchive

.claude/
